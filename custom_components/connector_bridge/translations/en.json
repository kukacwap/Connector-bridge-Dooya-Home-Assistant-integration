{
  "config": {
    "step": {
      "user": {
        "title": "Set up Connector Bridge",
        "description": "Find your API key in the Connector+ app: **Settings → About → tap 5 times anywhere**.",
        "data": {
          "host": "Bridge IP address",
          "key": "API key"
        }
      },
      "reconfigure": {
        "title": "Reconfigure Connector Bridge",
        "description": "Update the bridge IP address or API key. The bridge must be reachable at the new address.",
        "data": {
          "host": "Bridge IP address",
          "key": "API key"
        }
      }
    },
    "error": {
      "cannot_connect": "Could not connect to the bridge. Check the IP address and key.",
      "unknown": "Unexpected error. Check the logs."
    },
    "abort": {
      "already_configured": "This bridge is already configured.",
      "reconfigure_successful": "The bridge configuration was updated."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Blind travel times",
        "description": "Full open/close travel time in seconds for each blind (by MAC address). Used to estimate position for motors that don't report one, so the state shows correctly in HomeKit."
      }
    }
  }
}
