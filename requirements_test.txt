# Test-only dependencies. pytest-homeassistant-custom-component pins a
# matching Home Assistant core and pytest, so nothing else needs pinning here.
pytest-homeassistant-custom-component==0.13.316

# Declared in manifest.json; installed here so the bridge module imports.
pycryptodomex==3.21.0

# Used by the services.yaml / translation consistency tests.
PyYAML>=6.0
